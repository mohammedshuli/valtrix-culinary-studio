// Supabase integration removed from project.
// This file remains as a lightweight placeholder to avoid import errors in components.

export const supabase = {
  from: async () => ({ data: [], error: null }),
};